[pytest]
asyncio_mode = auto
markers =
    integration: exercises real business logic against a real Postgres database (docker compose up -d postgres). Skips gracefully, not a failure, if that database isn't reachable.
